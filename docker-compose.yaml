version: '3.8'
volumes:
  postgres_data:
  static_value:
  media_value:

services:
  # описание контейнера db
  db:
    image: postgres:12.4
    restart: always
    volumes:
<<<<<<< HEAD
      - postgres_data:/var/lib/postgresql/date/
    env_file:
      - .env
=======
      - postgres_data:/var/lib/postgresql/data

>>>>>>> 6cb657e25ce2794095afe037f3690be772aed13d
  web:
    #image: 2498573548934587/yamdb_final:latest
    build: .
    restart: always
    command: gunicorn api_yamdb.wsgi:application --bind 0.0.0.0:8000
    ports:
      - "8000:8000"
    volumes:
      - static_value:/code/static/
      - media_value:/code/media/
    depends_on:
      - db
    env_file:
      - .env

  nginx:
    image: nginx:1.19.3

    ports:
      - "80:80"

    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf

      - static_value:/var/html/static/

      - media_value:/var/html/media/

    depends_on:
      - web

volumes:
  postgres_data:
  static_value:
  media_value:
